version: "2"
sql:
  - engine: "postgresql"
    queries: "database/queries"
    schema: "database/schema.sql"
    gen:
      go:
        package: "repo"
        out: "repositories"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_prepared_queries: true
